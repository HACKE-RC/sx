#!/usr/bin/env python3
from pathlib import Path
import sys

_SRC = Path(__file__).resolve().parent / "src"
if _SRC.is_dir():
    sys.path.insert(0, str(_SRC))

from sx_search.cli import main

raise SystemExit(main(__import__("sys").argv))
